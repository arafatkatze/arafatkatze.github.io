---
layout: page
permalink: /404.html
title: "Page not found"
description: "You've wandered off the trail — but the stars still know the way home."
nav: false
---

<div class="lost-404" id="lost-404">

  <div class="lost-scene">
    <div class="lost-stars" id="lost-stars" aria-hidden="true"></div>
    <div class="lost-moon" aria-hidden="true"></div>
    <div class="lost-shooting" aria-hidden="true"></div>
    <div class="lost-number" aria-hidden="true">404</div>

    <svg class="lost-hills" viewBox="0 0 1200 480" preserveAspectRatio="xMidYMax slice" aria-hidden="true">
      <defs>
        <radialGradient id="lostLanternGlow" cx="50%" cy="50%" r="50%">
          <stop offset="0%"  stop-color="#ffd98f" stop-opacity="0.95" />
          <stop offset="35%" stop-color="#ff9d4d" stop-opacity="0.45" />
          <stop offset="100%" stop-color="#ff9d4d" stop-opacity="0" />
        </radialGradient>
        <radialGradient id="lostGroundGlow" cx="50%" cy="50%" r="50%">
          <stop offset="0%"  stop-color="#ffb15c" stop-opacity="0.40" />
          <stop offset="100%" stop-color="#ffb15c" stop-opacity="0" />
        </radialGradient>
      </defs>

      <!-- distant ranges -->
      <path class="lost-hill-back" d="M0,250 C200,170 400,210 600,180 S1000,160 1200,210 L1200,480 L0,480 Z" />
      <path class="lost-hill-mid"  d="M0,320 C250,255 450,305 700,265 S1050,255 1200,305 L1200,480 L0,480 Z" />

      <!-- a few lonely pines on the far slope -->
      <path class="lost-tree" d="M955,300 l-10,26 l20,0 Z M955,288 l-8,20 l16,0 Z" opacity="0.85" />
      <path class="lost-tree" d="M1040,312 l-8,22 l16,0 Z" opacity="0.8" />

      <!-- the ridge the wanderer stands on -->
      <path class="lost-hill-front" d="M0,400 C200,360 400,382 560,352 C720,322 900,362 1200,360 L1200,480 L0,480 Z" />

      <!-- pines near the trail -->
      <path class="lost-tree" d="M180,372 l-11,30 l22,0 Z M180,358 l-9,24 l18,0 Z" />
      <path class="lost-tree" d="M300,390 l-8,22 l16,0 Z" />

      <!-- the wanderer + lantern -->
      <g transform="translate(560,352)">
        <!-- warm light pooling on the ground -->
        <ellipse cx="14" cy="-2" rx="95" ry="13" fill="url(#lostGroundGlow)" />
        <!-- halo around the lantern -->
        <circle class="lost-lantern-glow" cx="20" cy="-70" r="34" fill="url(#lostLanternGlow)" />

        <!-- silhouette -->
        <g class="lost-figure" stroke-width="6">
          <!-- legs, mid-stride -->
          <path d="M0,-26 L-7,0" />
          <path d="M0,-26 L9,-3" />
          <!-- torso -->
          <path d="M0,-26 L-1,-47" stroke-width="8" />
          <!-- raised arm to the lantern pole -->
          <path d="M-1,-45 L13,-40" stroke-width="5" />
          <!-- lantern pole, held aloft -->
          <path d="M13,-40 L20,-66" stroke-width="2.5" />
        </g>
        <!-- head -->
        <circle class="lost-figure-fill" cx="-1" cy="-54" r="6" />
        <!-- backpack -->
        <rect class="lost-figure-fill" x="-9" y="-45" width="7" height="13" rx="3" />

        <!-- the lantern itself -->
        <g class="lost-lantern-glow">
          <rect class="lost-lantern-core" x="16" y="-72" width="8" height="9" rx="1.6" />
          <path d="M16,-72 L24,-72" stroke="#04020a" stroke-width="1.4" />
          <path d="M20,-74 L20,-72" stroke="#04020a" stroke-width="1.4" />
        </g>
      </g>
    </svg>
  </div>

  <div class="lost-copy">
    <p class="lost-eyebrow">Error 404 · off the map</p>
    <h1 class="lost-title">You’ve wandered off the trail.</h1>
    <div class="lost-actions">
      <a class="lost-btn lost-btn--home" href="{{ '/' | relative_url }}">Follow the lantern home</a>
    </div>
  </div>

</div>

<script>
  (function () {
    // scatter a real little sky
    var sky = document.getElementById("lost-stars");
    if (sky) {
      var frag = document.createDocumentFragment();
      for (var i = 0; i < 90; i++) {
        var s = document.createElement("span");
        s.className = "lost-star";
        var size = (Math.random() * 2 + 0.6).toFixed(2);
        s.style.width = size + "px";
        s.style.height = size + "px";
        s.style.left = (Math.random() * 100).toFixed(2) + "%";
        s.style.top = (Math.random() * 70).toFixed(2) + "%"; // keep them in the sky
        s.style.setProperty("--dur", (Math.random() * 4 + 2.5).toFixed(2) + "s");
        s.style.setProperty("--delay", (Math.random() * 5).toFixed(2) + "s");
        frag.appendChild(s);
      }
      sky.appendChild(frag);
    }
  })();
</script>
